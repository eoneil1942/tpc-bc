SQL: SELECT  h_b_id, sum(h_escrowdelta) FROM HISTORY GROUP BY H_B_ID;
COST: 6000000.0
PLAN:

RETURN RESULTS TO STORED PROCEDURE
 AGGREGATION ops: sum
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    AGGREGATION ops: sum
     SEQUENTIAL SCAN of "HISTORY"

