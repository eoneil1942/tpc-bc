SQL: SELECT b_id, sum(b_escrowbalance) FROM BRANCH GROUP BY B_ID;
COST: 6000000.0
PLAN:

RETURN RESULTS TO STORED PROCEDURE
 AGGREGATION ops: sum
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    AGGREGATION ops: sum
     SEQUENTIAL SCAN of "BRANCH"

