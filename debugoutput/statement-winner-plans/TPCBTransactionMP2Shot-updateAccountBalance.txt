SQL: UPDATE ACCOUNT SET A_ESCROWBALANCE = ? WHERE A_ID = ? AND A_B_ID = ?;
COST: 18.0
PLAN:

RETURN RESULTS TO STORED PROCEDURE
 AGGREGATION ops: sum
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    UPDATE
     INDEX SCAN of "ACCOUNT" using "SYS_IDX_A_PK_TREE_10021" (unique-scan covering)

